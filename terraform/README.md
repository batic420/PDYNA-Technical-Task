# Terraform Example Codebase
- Uses local modules (use remote modules in real world scenarios)
- Uses a separate `projects/` folder (use separate repositories for applications with remote modules in real world scenarios)
- Codebase is completely empty, you can find everything you need in the `tasks/` folder